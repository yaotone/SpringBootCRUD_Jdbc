spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/UsersDB
    username: postgres
    password: "0000"

  mvc:
    hiddenmethod:
      filter:
        enabled: true